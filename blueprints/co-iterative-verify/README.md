# Blueprint: co-iterative-verify

Self-contained TechDemos blueprint (engine schemaVersion 1).

- `blueprint.json` — DAG topology (authoritative: id / title / version / nodes / edges)
- `skills/<nodeId>/SKILL.md` — bundled skills (one dir per node id)

Run via the harness UI (🗺️ 工作流 panel) or the `/_blueprint/*` HTTP API.
